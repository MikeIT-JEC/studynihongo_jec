<% provide(:title, "Cebu Schedules |") %>
<%= render "layouts/navbar_admin" %> 
<div class="container"> 
	<div class="section">      
	  	<h4 class="center">SCHEDULE LIBRARY</h4>

	  	<%= render "partials/notification" %>

	  	<% if @cebuscheds.count == 0 %>
			<h3 class="red-text center">No Schedules created yet!</h3>
			<p class="center"><%= link_to "Create now!", cebu_new_schedule_path, :class => "waves-effect waves-light btn-large blue" %></p>
		<% else %>
			<div class="row"> 
				<% @cebuscheds.each do |cs| %>
				  <div class="col s12 m4">
				  	<% if cs.status == 1 %>
				    	<div class="card teal darken-1 center-align">  
				  	<% else %> 
				    	<div class="card red darken-1 center-align">  
				  	<% end %>
				  		<div class="card-content">
				      	  <span class="card-title">
					      	<%= link_to cs.duration.upcase,{controller: "cebu_sub_scheds", action: "new", id: cs.slug}, :class => "white-text"%>
					      </span> 
					      <p class="white-text">Created: <%= time_ago_in_words(cs.created_at) %> ago</p>
				  		</div> 
				  		<div class="card-action"> 
						    <%= link_to material_icon.edit, {controller: "cebuscheds", action: "edit", id: cs.slug}, :class => "white-text"%>  
					      	<%= link_to material_icon.delete, {controller: "cebuscheds", action: "destroy", id: cs.id}, :data => {confirm: "Do you want to delete this?"}, :class => "white-text"%> 
				  		</div>
				    </div>
				  </div> 
				<% end %>
			</div>
		<% end %>
		<div class="fixed-action-btn">
			<%= link_to material_icon.add, cebu_new_schedule_path, :class => "waves-effect waves-light btn-floating btn-large blue"%> 
		</div>  
	</div>
</div>

<div class="section">
	<div class="container center">
		<%= link_to "Main dashboard", admin_dashboard_path %> > 
		<%= link_to "Cebu Dashboard", cebu_dashboard_path %>
	</div>
</div>